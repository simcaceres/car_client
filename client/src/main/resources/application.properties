spring.application.name=client

# ? LÍNEA 2: Solo la URL (fíjate que termine limpio en sslmode=require)
spring.datasource.url=jdbc:postgresql://dpg-cpsgsk08fa8c739m1a50-a.oregon-postgres.render.com/credito_card_db?sslmode=require

# ? LÍNEA 3: El usuario va abajo, solo y al principio de la línea
spring.datasource.username=credito_card_user

# ? LÍNEA 4: La contraseña va en su propia línea
spring.datasource.password=g92nQnN2R3Q1z2wreX6YhZqA8k696zPj

# ? ASIGNAR EL PUERTO QUE RENDER EXIGE
server.port=${PORT:8080}

# ?? CONFIGURACIONES DE JPA (Cada una en una línea distinta)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect