# ? URL DE CONEXIÓN (Asegúrate de que termine limpiamente en sslmode=require)
spring.datasource.url=jdbc:postgresql://tu_servidor_de_render.com:5432/tu_base_de_datos?sslmode=require

# ? USUARIO (En una línea totalmente nueva, sin espacios raros antes)
spring.datasource.username=credito_card_user

# ? CONTRASEÑA
spring.datasource.password=tu_contraseña_aqui

# ?? CONFIGURACIONES EXTRA DE JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true